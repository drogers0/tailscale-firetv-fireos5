diff --git a/android/build.gradle b/android/build.gradle
index 8f64b3d..7031591 100644
--- a/android/build.gradle
+++ b/android/build.gradle
@@ -51,6 +51,10 @@ android {
     }
 
     compileOptions {
+        // java.time (Duration/Instant/DateTimeFormatter) is API 26 on Android and is used
+        // throughout TimeUtil and MainViewModel. Desugaring backports it rather than
+        // patching every call site.
+        coreLibraryDesugaringEnabled true
         sourceCompatibility JavaVersion.VERSION_17
         targetCompatibility JavaVersion.VERSION_17
     }
@@ -108,6 +112,8 @@ android {
 }
 
 dependencies {
+    coreLibraryDesugaring 'com.android.tools:desugar_jdk_libs:2.0.4'
+
     // Android dependencies.
     implementation "androidx.core:core:1.13.1"
     implementation 'androidx.core:core-ktx:1.13.1'
